# 更新日志

## [1.0.8] - 2026-09-26

### 新增

- `/lumen webui-host` 指令：在控制台切换 WebUI 监听地址（`local` = 仅本机、`public` = 所有接口、也可指定具体 IP），立即生效 (#1)
- WebUI 配置页支持检测配置文件外部修改：手动编辑 config.json 后自动热生效并刷新页面，无需重启或手动刷新

### 修复

- 适配器开关关闭后重新打开仍显示开启、配置文件未实际修改的问题
- QQ 官方机器人适配器群服互通设置无法保存、重开弹窗仍为旧值的问题
- 官方 QQ 机器人绑定主群后，总览页仍显示未配置的问题
- WebUI 左下角语言切换按钮不生效、只能改配置文件的问题
- WebUI 监听地址设为 0.0.0.0 后，启动日志仍显示 127.0.0.1 的问题 (#1)
- 启动时控制台 Logo 错乱的问题
- 总览页「OneBot 连接」文案改为「适配器状态」
- 适配器重载日志不再对 QQ 官方等非 OneBot 适配器误报「OneBot 连接」
- WebUI 连接管理中「WebSocket」适配器字样统一改为「OneBot」

## [1.0.7] - 2026-09-19

### 新增

- **子插件图标库扩展（7 → 34 个 + emoji 支持）**：`registerPage(icon=...)` 在原有 model/bot/chat/shield/spark/gear/chart 之外新增 27 个内置图标（home/user/users/server/database/map/box/gift/trophy/crown/coin/fire/zap/heart/star/bell/clock/calendar/music/image/search/link/lock/book/code/terminal/globe），全部为与主面板同风格的折线 SVG；传入 emoji（或其它非 ASCII 字符）按字符渲染，任意 emoji 均可使用（建议单个）；其它值或缺省回退默认 SVG 图标。子插件开发文档的图标规则与示例已同步更新
- **登录页密码框明暗切换重制**：眼睛按钮改为双图标同格交叉淡入 + 缩放过渡；明文/掩码两种状态的字体与内边距完全一致，切换零位移、零抖动；补齐 `aria-pressed` 键盘可达性，Enter 登录逻辑迁移至事件委托

### 安全修复

- **Xbox 玩家名字符集校验**：绑定白名单输入的玩家名现仅允许字母、数字、空格、下划线、连字符与点号（1-32 位），可破坏 `whitelist add/remove` 命令语法的畸形输入在入口即被拒绝并提示（三语已翻译）；历史绑定数据不受影响，解绑不受限
- **反向 WebSocket 无鉴权监听告警**：未配置 access_token 且监听地址为非环回时，反向服务启动即输出安全警告——任何可达该端口的客户端均可注入伪造事件（可能触发管理员命令）；建议配置令牌或仅监听内网地址
- **callPluginCommand 参数解析加固**：动作参数改为先按逗号切分、再逐段变量替换，参数值（如消息文本）内含逗号不再被错误拆分为多个参数

### 修复

- **桌面端子插件自定义图标不生效**：桌面侧栏与移动端「其它」面板两处硬编码了默认地球图标，忽略子插件 `registerPage(icon=...)` 传入的 icon；现抽取统一渲染函数，命名图标 SVG / emoji 字符 / 默认图标三态在所有布局（竖屏底栏、横屏侧栏、桌面侧栏、「其它」面板）下渲染一致
- **手机横屏语言切换按钮溢出**：窄侧栏下 2×2 全文字语言按钮因 CSS 级联被意外恢复，塞爆 64px 侧栏；现以更高优先级规则固定为单列迷你短码按钮（AUTO / 简 / EN / 繁），横竖屏均不再溢出

### 优化

- **全代码库注释精简**：约 30 个核心文件清理审计式批注约 760 行（历史流水、复述代码、多行设计叙述压缩为单行"为什么"），docstring、安全约束、协议怪癖与魔法数字说明全部保留；无任何行为变更（392 项测试全量回归通过）
- `connections.py` 中以条件表达式执行副作用的语句改为标准 `if` 语句，提升可读性
- 内置 `websockets` 库由 15.0.1 升级至 17.1（上游最新稳定版），加载策略不变（仍为磁盘合格版本优先、内嵌兜底）；随 Endstone 官方基线将 `requires-python` 提升至 `>=3.11`，README 与使用文档的环境要求已同步更新

## [1.0.6] - 2026-09-12

### 新增

- **前端样式共享层 `/lumen.css`**：主面板样式拆分为「共享层 + 主面板层」两级（`lumen.css` / `app.css`）。子插件自定义页面引用共享层即获得与主面板一致的主题观感（深浅色跟随系统），无需自备样式；也支持「共享层 + 页内扩展」或完全自写（详见子插件开发文档）。共享层为公开 API，不兼容变更将升主版本号
- **群绑定密钥（免手填绑定群与管理员）**：适配器编辑「权限与身份设置」新增「获取绑定密钥」入口。面板签发一次性密钥（仅显示一次、5 分钟自动过期、生成新钥即焚毁旧钥、绑定成功即消费），支持一键复制绑定指令；管理员在目标群发送「/lumen绑定 密钥」即可自动把当前群与发送者写入群列表和管理员列表（@ 或不@ 机器人均可；简体 `/lumen绑定`、繁体 `/lumen綁定`、英文 `/lumen bind` 指令变体通吃，大小写不敏感）；多群绑定时群与管理员自动去重不重复填写；QQ 官方机器人域同样适用（group_openid / openid）
- **群内 @ 批量添加管理员**：管理员在群内发送「/添加管理员 @用户 @用户 …」即可把被 @ 的用户批量写入当前适配器的管理员列表，QQ 号 / openid 自动从消息中的 @ 提及获取（兼容 OneBot 消息段与 CQ 码两种格式），无需再手抄号码到面板填写；非管理员使用会被拒绝，重复添加自动去重

### 修复

- **聊天美化插件导致游戏消息不转发到群（加固重制）**：美化 / 格式化类插件（如 u_beautiful_chat、UBC）会取消原版聊天事件再以自己的格式重发，旧版会因此丢消息。聊天吸收探针重制为**时间戳回溯**机制：无条件记录广播并打时间戳，原生聊天事件到达时回看时间窗内是否出现过包含原始发言的广播，据此判定「已被美化插件吸收展示」并照常转发玩家原始发言（游戏内格式的修饰不带进群消息）。该机制不再依赖事件分发顺序与线程局部状态，对基岩版修改聊天的 addon 兼容性更好
- **新增 `chat.forward_cancelled` 转发策略**：被取消的聊天事件如何处理，支持「自动（按展示证据判定）/ 始终转发 / 取消即不转发」三档，WebUI 配置页新增「聊天转发」独立导航与下拉选项（auto / always / never，三语均已翻译）。无广播信号的「逐玩家私发」型美化插件可用「始终转发」兜底
- **回到顶部按钮点击后不消失**：平滑滚动结束后按钮仍悬停在右下角的漏网口
- **适配器编辑页面卡顿**：弹窗打开期间自动停用遮罩下方各层的毛玻璃效果（backdrop-filter），显著降低 GPU 负载，关闭弹窗即恢复
- **QQ 官方机器人静默日志失效**：开启「后台静默日志」后 `access_token 已刷新`、连接就绪等运行日志仍输出的漏网口

### 优化

- 框架更新旧 wheel 备份仅保留最近 3 份，不再无限累积
- 获取绑定密钥弹窗的倒计时改为纯色进度条 + 等宽数字（最后 60 秒切警示色），去除渐变动效
- 聊天事件链路诊断日志（`[chatdbg]`）统一纳入 `debug: true` 配置开关，调试模式关闭时完全静默

## [1.0.5] - 2026-09-06

### 修复（高危）

- **框架热重载崩服**：历史实现在调度器任务内执行 `Server.reload()`，而 reload 过程 `disablePlugins` → `cancelTasks` 会取消正在执行的任务自身，导致崩溃。热重载现在只能由管理员在命令上下文同步触发（`/lumen update framework -y`，与内建 `/reload` 同款安全路径）；`apply_framework_update` 只做下载校验与原子暂存，绝不触发重载

### 新增

* 框架更新「暂存 → 确认」两段式流程：新版本下载校验后原子暂存（含旧版备份，不会自动生效）→ WebUI 更新页「立即热重载」按钮（带确认弹窗）或服务器控制台 `/lumen update framework -y` 确认后生效

* WebUI 热重载过程 overlay：提示面板将短暂无法访问，完成后自动刷新；等待超时给出回滚排查指引（新版本启动失败会自动回滚旧版本，刷新页面即可恢复）

* `/api/health` 免认证健康检查端点（热重载后面板恢复探测）

* 周期性市场更新检查线程于 `on_disable` 置位停止信号，热重载/停服时干净退出，避免清理期间仍在触网/写暂存文件

* `staged_framework_update()` 本地暂存回执查询（不触网），供命令快速判断是否可直接热重载

### 子插件页面（iframe）体验修复

- **高度自适应失效修复**：旧版子插件页面自带的上报脚本会抢占幂等标志，导致新版高度上报从未安装、iframe 卡在兜底高度——改用独立哨兵标志；满屏型/短内容页面按「视口高 − 顶部位置」兜底下限，不再截断在底栏上方；重复调用幂等；修正 border-bottom 计量偏差；移动端手势在 iframe 内部滚动耗尽后链式传播到主文档

- **画布融合**：主面板向自定义页面注入融合基础样式（背景透明），页面不再呈现一块自带底色的「嵌入式网页方框」，与普通页面观感一致；需保留自定义背景可用内联 `!important` 覆盖

### 优化

* 注册类日志（插件 API / 配置表单 / 自定义页面注册提示）降为 debug 级别，仅 debug 配置开启时输出，不再刷屏

* 敏感词过滤：词条重复、正则语法错误的明确提示与一键清除

* 子插件配置搜索显示「{n} 项匹配」计数

* 依赖约束（requires）版本比较口径明确为宽松元组比较（`1.2.0 > 1.2`，视为更具体/更新），与框架更新比较的「补 0 对齐」口径分离，docstring 与测试同步说明

## [1.0.4] - 2026-08-29

### 新增

* 子插件配置页面完整复刻 AstrBot 插件配置页风格：分组嵌套卡片 + 左信息右控件两列行式布局（窄屏自动上下堆叠），保留既有全部功能

* `ConfigFormBuilder` 新增字段能力：`section(title, desc)` 分组卡片、`multiselect` 多选复选框组、`text(secret=True)` 密码框（带明文切换）、`number(minimum/maximum/step)` 滑块联动；全部字段支持 `default`（显式默认值）、`obvious_hint`（‼️ 重要标记）、`show_key`（显示键名）关键字参数

* 配置面板搜索过滤：按 key / label / desc 模糊匹配，无命中的分组整组隐藏，无结果时显示空态提示

* 单项恢复默认：当前值 ≠ 默认值的字段右侧出现恢复按钮（未显式声明 default 时以注册时初始值为默认），底部提供一键全部恢复（带确认）

* 未保存状态管理：任意输入即标记「未保存的更改」（底部脉冲徽章），关闭弹窗前拦截确认；支持 Ctrl/Cmd+S 快捷保存

* array 字段升级为 chips 列表编辑器：逐项增删（回车添加、✕ 删除）+ 按行批量导入面板

* textarea 字段提供右上角全屏编辑弹窗

* 配置面板全部新交互的三语言（zh\_CN / zh\_TW / en）文案

### WebUI 优化

* `select` 字段改用自定义下拉组件（带搜索），options 由后端统一规范化为 `{"value","label"}` 列表

* 配置弹窗标题栏显示配置项数量徽章

### 审计修复

- QQ 官方适配器：断线 Resume 重放消息按 seq 去重（修复重连期间机器人复读）；msg_id 过期（40034005）被动凭据及时清理并剥离过期字段改走主动通道重试；网关 op=9 无效会话时 seq 归零走全新 Identify
- 子插件加载器：同名子插件冲突仅先加载者生效；`reload_one` 先校验新清单再卸载旧实例（清单损坏 / load:false 时运行实例不被误卸载）；升级递归保留嵌套目录用户数据；`set_enabled` 启用/禁用即时生效（立即加载/卸载运行实例）
- 正则引擎：规则中非法动作元素跳过并记日志，不再中断整条规则
- 插件市场：清单检查时间原子写入（临时文件 + 替换），不再残留 `.json.tmp`

### 修复

- 批量导入面板显隐 CSS 错位（规则写在 textarea 上而非包裹容器，导致面板无法展开）

### 文档

* 子插件开发文档「配置表单」章节重写：字段类型表（含 section/multiselect/secret/滑块参数）、通用可选参数表、WebUI 渲染特性说明、完整示例（分组 + 多选 + 滑块 + secret）

### 新增（深度审计第二轮）

* QQ 官方机器人 OneBot action 扩展：`set_group_ban`（标准禁言，duration 秒，0=解禁，最长 30 天）、`set_group_ban_batch`（官方批量禁言，单次最多 10 人）、`get_group_restrict_chat_setting`（禁言规则与禁言列表查询）、`get_group_bot_state`（机器人群内角色/主动消息开关）、`get_group_join_request_list`（待审批入群申请分页拉取）；`delete_msg` / `set_group_add_request` 支持结果 callback 回传真实成败（`{"ok": true}` / `{"ok": false, "error": ...}`）

* @机器人 留痕（`mention_self`）：`GROUP_AT_MESSAGE_CREATE` 事件类型本身即「@机器人」信号（官方服务端会从 content 中剥离 @bot 前缀，正文扫描恒漏），全量消息模式下兜底扫描正文 mention 段；子插件可据此实现 @ 唤醒判定

* 子插件自定义页面（iframe）体验升级：内容区全宽铺开（不再被固定宽度方框框住）、iframe 高度随内容自适应随主面板滚动、确认弹窗与 toast 经 postMessage 委托主面板顶层呈现（iframe 内 fixed 元素无法超出边界、自适应高度后会漂移）

* GitHub Actions 自动构建工作流：推送 `v*` 标签自动构建 wheel 并创建 GitHub Release 附带产物；支持手动触发（workflow\_dispatch）

### WebUI 性能优化

* 静态资源 gzip 压缩（html/js/css/json/svg/txt/md，压缩结果缓存，仅压缩后更小的资源），页面首次加载体积大幅下降

* ETag / 304 协商缓存：静态文件按 size+mtime 生成 ETag，命中 If-None-Match 直接返回 304；分类型 Cache-Control 策略（png/ico/svg 长缓存 1 天，html/js/css no-cache 保证升级后及时更新）

### 审计修复（高危）

- 聊天屏蔽词打码崩溃：`str.lower()` 对个别 Unicode（如 `İ` U+0130）会展开为多字符，归一化文本与原文不等长导致按索引回切原文时越界崩溃/打码错位；改为逐字符归一化并构建索引映射，命中区间精确映射回原文

- 版本比较段数缺陷：裸元组比较中 `(1,2) < (1,2,0)`，相等版本被误判不满足（`==1.2` 拒装已装的 `1.2.0`、`min_v 1.2.0` 拒载宿主 `1.2`、市场把同版本误报「有更新」）；统一改为补 0 对齐比较（requires / loader min\_v / ZIP 升级拦截 / marketplace 三处同源修复）

- 子插件定时任务泄漏：热重载清理时把 Task 对象传给期望任务 ID 的 `scheduler.cancel_task`，任务从未真正取消导致重复执行；改为直接 `task.cancel()` 并按 task\_id 从注册表移除

- 屏蔽词正则词条 ReDoS 防护：复用正则引擎的风险 pattern 启发式（嵌套量词 / 超大重复下界），灾难性回溯词条拒绝编译并告警

### 性能修复

- 屏蔽词打码路径：逐词条重复归一化整段文本 + 每次重新 `re.compile` 改为合并正则一次 `finditer` 定位全部命中区间 + 复用预编译 pattern + 区间合并一次性重建（消除替换顺序依赖）；万级词条打码从逐词 O(n·m) 降为常数次全文扫描，2 万词条实测单次 0.015ms

- 合并正则长词优先排序：交替分支按出现顺序尝试，短词在前会遮蔽长词（「敏感|敏感词」只能命中前者），按长度降序保证打码覆盖完整

### 代码清理

- 移除 connections.py 无引用的 `_NAME_RE`（匹配任意非空串且零调用）与 loader.py 重复的 `_version_tuple`（统一到 requires 模块单一实现）

### 文档

* 子插件开发文档新增「QQ 官方机器人的 action 支持」章节：官方端点映射表（含限制说明）、官方扩展 action 清单、禁言调用示例

## [1.0.3] - 2026-08-21

### 新增

* LumenBridge 框架本体在线更新：从市场检查新版本 → 下载并 SHA-256 校验后暂存（幂等，已暂存不重复下载）→ 热重载生效。热重载走 Endstone 官方 `Server.reload()`（与 `/reload` 命令同一实现）；旧版 Endstone 无该绑定时自动回退派发 `reload` 命令

* `/lumen update framework [-y]` 命令（两步确认）：不带 `-y` 只检查新版本并下载校验暂存（不更新）；带 `-y` 才执行更新并热重载。热重载会重载服务器内**全部**插件（等同 `/reload`），故 `updates.auto_update` 对框架本体只通知不自动更新，必须由管理员经命令或 WebUI 确认弹窗手动确认（弹窗明确提示将重载所有插件、面板短暂下线后自动恢复）

* 热重载失败自动回滚：移除失败的新 wheel → 放回备份旧 wheel → 再次触发 reload 恢复旧版本运行

* 三语言（zh\_CN / zh\_TW / en）框架更新命令与 WebUI 确认弹窗文案

* 子插件强制依赖（`requires`）：`lumen.json` 新增 `requires` 字段，可声明对其它子插件与 Endstone 插件的强制依赖（支持 `>= <= > < == !=` 六种版本约束，比较符与版本号成对出现）。加载时按依赖拓扑排序（被依赖者先加载，循环依赖按原顺序各自报错）并强制检查：Endstone 插件依赖缺失时报错提示用户先安装并启用；子插件依赖缺失时在市场安装/升级/「补装依赖」时自动从市场按 `manifest_name` 精确匹配补装（像 pip 依赖一样递归安装，带循环依赖检测与版本约束选择，市场中找不到时提示手动安装）

* 卸载/禁用子插件前的反向依赖预检：其它子插件依赖目标插件时列出依赖方并警告

* 插件市场端配套（需同步更新市场 PHP 程序）：上传 ZIP 时校验 `requires` 格式（非法声明拒绝发布）并按版本存储；插件列表 API 新增 `manifest_name` 精确搜索参数；公开 API 在插件列表/详情/版本接口输出 `manifest_name` 与 `requires`；社区页插件详情与后台审核视图展示依赖信息

* `/lumen update <子插件名>` 命令：更新指定子插件到最新版

* `/lumen update -A|--all` 命令：一键更新全部有新版本的市场子插件（单个失败不阻断其余，结果逐个回显）

* WebUI「检查插件更新」升级：发现新版本时弹出「可更新的子插件」卡片列表（支持选择目标版本单独更新，仅可更新到更高版本），底部支持一键更新全部；均为最新版时弹窗提示

* 子插件更新相关三语言（zh\_CN / zh\_TW / en）文案与命令权限配置项（`commands.update.allow_player`）

* WebUI 慢速读取（Slowloris）防护：请求体读取按 16KB/s 最低速率设置总超时，杜绝慢速滴漏攻击占满连接线程

### WebUI 优化

* 子插件卡片新增「插件依赖」徽章与「插件依赖缺失」红色标识，缺失时提供一键「补装依赖」入口（子插件依赖自动从市场安装，Endstone 插件依赖提示手动安装）

* 插件市场详情页版本列表展示「子插件依赖 / Endstone 插件依赖」

* 横屏（电脑端）子插件页面卡片改为两列并排，窄屏保持单列

* 子插件卡片大小严格一致：列轨道改用 `minmax(0, 1fr)` 切断长描述/长名称对列宽的影响，描述固定两行占位、标题行单行省略号截断，全部卡片等宽等高（名称截断后悬停可看全名）

* 插件市场图标修复为固定正方形（原先被拉伸为长方形）

* 「已是最新版」弹窗按钮移至右侧

### 修复

* 子插件升级数据丢失：改为递归备份全部非代码文件（原仅备份顶层白名单后缀文件）

* 市场清单检查时间改为原子写入，进程中断不再留下不完整文件

* 市场端依赖字段类型异常不再中断整个更新检查流程

* `/lumen pip`、`/lumen update` 命令在实例未完成初始化时崩溃：新增安全日志访问器逐级回退（tee logger → logger → 模块级 logger），后台线程日志不再丢失

* WebUI 安装端点（上传/链接安装）未持主线程操作锁可并发执行：补锁后冲突请求返回 409，防止并发主线程操作

* pip 卸载同步阻塞 WebUI 线程最长 60 秒：移入后台任务队列与进度跟踪（与安装一致）

* QQ 官方适配器事件在 WebSocket 线程内同步处理导致心跳停跳：新增独立派发队列与工作线程（与 WS 适配器同构），消除重连风暴

* WebSocket 派发线程退出泄漏：补双退出条件（哨兵值 + 运行标志），队列满丢哨兵时线程仍可退出

* 白名单文件损坏时被直接覆写丢失全部绑定：覆盖前自动备份 `.corrupt` 文件

* 子插件 ZIP 安装包解压无限制（ZIP 炸弹）：新增条目数 / 单文件大小 / 总解压大小三级限额

* 子插件依赖版本约束解析失败：`~=` 兼容版本比较与通配符匹配按 PEP 440 实现

* 后台线程（pip 安装/卸载、市场更新）直接调用原始 logger 在 Windows 下与控制台输入线程竞争导致服务端崩溃：统一改用线程安全 tee logger

### 日志

* QQ 官方机器人「连接成功，群服互通已就绪」「会话已恢复（Resume 补投完成）」纳入后台静默日志开关（`suppress_connection_log`，默认开启）：连接/重连不再刷屏，排障时可在连接管理卡片关闭开关查看全部日志；WS/AstrBot 适配器行为不变

## [1.0.2] - 2026-08-20

### 新增

* QQ 官方群聊 @ 提及恢复正常显示（适配官方新格式，旧格式弃用不受影响）

* 存储目录化：适配器配置拆分到 `connections/` 分文件存储，运行数据统一迁入 `data/` 目录

* 旧版数据一键迁移脚本 `scripts/migrate_storage.py`（支持预览，自动备份）

### WebUI 优化

* 插件市场卡片：图标居左、文字并排，按钮移至右下角，布局更紧凑

* 子插件卡片：操作按钮收进「⋯ 更多」菜单，消除按钮堆砌；描述超长时折叠，卡片大小统一

* 更新流程：单一更新按钮，可选市场更新（支持选择历史版本）或手动更新；已是最新版时弹出美化提示

* 安装/更新进度弹窗现在包含依赖安装的进度与日志

### 修复

* `/lumen pip install` 命令无法使用

* 关闭 WebUI 后 `/lumen reload` 不生效，且会覆写用户配置

* 手改 config.json 出错时被默认配置覆写（现自动备份损坏文件）

* 适配器名超长、旧配置迁移后静默断连、损坏 lumen.json 阻断加载等问题

* 反向 WebSocket 多客户端在线状态不对称、事件误判重复丢弃

* 群聊广播单个目标失败中断整轮发送、消息超长截断协议错误

* 跨线程日志丢失、pip 安装失败无提示等运维问题

* 连接日志开关（`suppress_connection_log`）语义明确化，默认静默

## [1.0.1] - 2026-08-18

* 初始审查版本

